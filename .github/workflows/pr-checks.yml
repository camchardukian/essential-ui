name: PR Checks

on:
  pull_request:
    branches:
      - main

jobs:
  cicd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./templates/common-steps.yml
        with:
          NAME: ${{ github.workflow }}
